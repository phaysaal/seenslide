# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ["python312", "gcc", "gnumake", "rustc", "cargo", "pipenv"]

[start]
cmd = "python -m modules.cloud.server --host 0.0.0.0 --port $PORT"
